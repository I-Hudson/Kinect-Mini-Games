using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class DebugHandPosition : MonoBehaviour
{
	public GameObject text;

	// Update is called once per frame
	void Update()
	{
	//	if ( KinectManager.Instance.IsUserDetected() )
	//	{
	//		text.GetComponent<Text>().text = KinectManager.Instance.GetRawSkeletonJointPos( KinectManager.Instance.GetPlayer1ID(), ( int )KinectWrapper.NuiSkeletonPositionIndex.HandRight ).ToString();
	//	}
	}
}
